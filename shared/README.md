# Shared

Contratos compartidos entre `frontend/` y `backend/`. Pensado para tipos TS que mapean al swagger (User, Auction, Bid, Fine, SellRequest, enums, etc.) — así el front consume la API con tipos fuertes y nada se duplica.

Si decidimos no compartir tipos, podemos borrar esta carpeta.
